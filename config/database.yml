default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: kidjucator_development

test:
  <<: *default
  database: kidjucator_test

production:
  <<: *default
  database: kidjucator_production
